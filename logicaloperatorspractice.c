#include <stdio.h>
#include <stdbool.h>

int main() {
    
    bool isSunny = false;

    if(!isSunny){
        printf("It is CLOUDY outside");
    }
    else{
        printf("It is SUNNY outside");
    }
    
    
    
    
    
    //int temp = 100000;

    //if(temp <= 0 || temp >= 30 ){          //and logical operator is &&, or is ||
          //printf("The temperature is BAD");
    //}
    //else{
        //printf("The temperature is GOOD");
    //}

    return 0;
}